{
  "name": "codex-remote-chat",
  "private": true,
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:mobile": "npm --prefix apps/mobile run start",
    "dev:web": "npm --prefix apps/web run dev",
    "build:web": "npm --prefix apps/web run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
